import threading


class Dog(threading.Thread)

    def __init__(self):
        super(Dog, self).__init__()
        # 年龄
            self.Age = 18
            # 生理状态
            self.PhyStatus = HUNGRY
            # 心理状态
            self. MentalStatus = PASSIVE
            # 人物认知
            self.Contact = {"name": "GYR", "relation": "Master", "favor": 100}


      def run(self):
      	 #一个进程处理自身内部状态变换
      	 #一个进程处理外部导致的状态变换
      	 
